{{{ logfile }}} 

{
  dateext
  dateformat {{{ rotateDateformat }}}
  olddir {{{ rotateDirectory }}}
  compress
  daily
  copytruncate
  nomail
  nosharedscripts
  rotate {{{ rotateCount }}}
  maxage {{{ maxageDays }}}
  notifempty
}
