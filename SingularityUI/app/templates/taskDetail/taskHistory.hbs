<div class="page-header">
    <h2>History</h2>
</div>
{{#if data.taskUpdates}}
    <table class="table table-striped">
        <thead>
            <tr>
                <th>
                    Status
                </th>
                <th>
                    Message
                </th>
                <th>
                    Time
                </th>
            </tr>
        </thead>
        <tbody>
            {{#each data.taskUpdates}}
                <tr>
                    <td>
                        {{humanizeText taskState}}
                    </td>
                    <td>
                        {{#if statusMessage}}
                            {{#isDecomissioningMessage statusMessage}}
                                <span class="attention">{{ statusMessage }}</span>
                            {{else}}
                                {{ statusMessage }}
                            {{/isDecomissioningMessage}}
                        {{else}}
                            &mdash;
                        {{/if}}
                    </td>
                    <td data-value="{{ timestamp }}">
                        {{timestampFromNow timestamp}}
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
{{else}}
    <div class="empty-table-message">This task has no history yet</div>
{{/if}}
