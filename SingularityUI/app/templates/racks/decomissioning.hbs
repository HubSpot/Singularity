<div class="page-header">
    <h1>Decommissioning</h1>
</div>
{{#if data.length}}
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>State</th>
                <th>Triggered by</th>
                <th>Started</th>
                <th>Uptime</th>
                <th data-sortable="false"></th>
            </tr>
        </thead>
        <tbody>
            {{#each data}}
                <tr>
                    <td>{{ id }}</td>
                    <td>{{ state }}</td>
                    <td>{{ decomissioningBy }}</td>
                    <td data-value="{{ decomissioningAt }}">
                        {{timestampFormatted decomissioningAt}}
                    </td>
                    <td data-value="{{ uptime }}">
                        {{timestampDuration uptime}}
                    </td>
                    <td class="actions-column">
                        <a data-rack-id="{{ id }}" data-action="remove" data-state="{{ state }}" title="Remove">
                             <span class="glyphicon glyphicon-trash"></span>
                        </a>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
{{else}}
    <div class="empty-table-message"><p>No decomissioning racks</p></div>
{{/if}}
