<header class="jumbotron subhead" id="overview">
    <h1>Scheduled Tasks</h1>
</header>

<section>
    <input type="search" placeholder="Search tasks..." required />
    <div class="row-fluid">
        <div class="span12">
            <table class="table sortTable">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Next run</th>
                        <th>JSON</th>
                    </tr>
                </thead>
                <tbody>
                    {{#eachWithFn tasks}}
                        <tr data-task-id="{{ id }}">
                            <td><span title="{{ id }}">{{#hardBreak name}}{{/hardBreak}}</span></td>
                            <td><span title="{{ nextRunAt }}">{{ nextRunAtHuman }}</span></td>
                            <td><a data-task-id="{{ id }}" class="dont-route view-json">JSON</a></td>
                        </tr>
                    {{/eachWithFn}}
                </tbody>
            </table>
        </div>
    </div>
</section>