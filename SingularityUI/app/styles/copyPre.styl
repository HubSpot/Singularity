@import nib
@import colors

pre.with-copy-button

    button[data-copy-button]
        cursor pointer
        background inherit
        border 0
        outline 0
        color $linkBlue
        text-align right
        margin: -8.5px
        padding 8.5px .7em 8.5px
        margin-right 1em

        &.zeroclipboard-is-hover
            background $linkBlue
            color #fff

        &:before
            content "Copy"

    &.copied

        button[data-copy-button]

            &, .zeroclipboard-is-hover
                background lighten($green, 50%)
                color darken($green, 43%)

            &:before
                content "Copied"