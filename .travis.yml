language: java
jdk:
  - oraclejdk7
  - openjdk7
  - oraclejdk8

script: mvn -Pbuild-swagger-documentation -B -fae verify

git:
  depth: 100

sudo: false

cache:
  directories:
  - $HOME/.m2
  - SingularityUI/bower_components
  - SingularityUI/node_modules
